.DS_Store
build/
DerivedData/
*.xcuserstate
xcuserdata/
.claude/settings.local.json
.qa-tmp/
.derivedData/
Archive/

# Secrets — never committed, never shipped (audio generation reads these
# offline; the app itself only plays bundled files).
.env
tools/.env
*.pem
# Raw generation staging (downloaded attachments before afconvert)
tools/.audio-raw/

